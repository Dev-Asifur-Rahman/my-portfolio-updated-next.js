import React from 'react';

const Hero = () => {
    return (
        <div id="go-top" className='w-full h-screen glass-bg rounded-lg md:rounded-xl'>
            Hero
        </div>
    );
};

export default Hero;